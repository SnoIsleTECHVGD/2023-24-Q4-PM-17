using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class damagetester : MonoBehaviour
{
    public statsManager playerAtm;
    public statsManager enemyAtm;
    private void Update()
    {
        if (Input.GetKeyDown(KeyCode.Escape)) ;
    }
}
